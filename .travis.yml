language: python
python:
  - "3.5"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
#script: nosetests
services:
- redis-server
before_script:
- "rq worker &"
script: python app_run.py
